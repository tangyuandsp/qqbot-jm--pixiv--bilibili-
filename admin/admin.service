[Unit]
Description=BiliBot Admin Panel
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/bilibot/admin
ExecStart=/usr/bin/python3 -m uvicorn admin_server:app --host 0.0.0.0 --port 8080
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
